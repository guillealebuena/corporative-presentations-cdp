Before/after pair — left is muted (previous), right is brand-colored (current), the delta sits between.

```jsx
<KpiCompare leftValue="6 min" leftLabel="Antes" rightValue="2 min" rightLabel="Ahora" delta="-66%" />
```
