/** 8px-thick progress ring with the percent centered. */
export interface KpiRingProps {
  percent: number;
  size?: number;
  color?: string;
}
export function KpiRing(props: KpiRingProps): JSX.Element;
