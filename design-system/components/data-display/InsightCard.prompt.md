Dark impact card with a diffuse mesh-gradient wash, an icon badge, a hero number + trend arrow, and three text tiers by size/opacity (value, label, context).

```jsx
<InsightCard icon="ChartLineUp" value="+38%" trend="up" label="Conversión" context="vs. trimestre anterior" />
```
