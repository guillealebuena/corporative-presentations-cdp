Card with a huge order number as a near-invisible background watermark — for ranked steps or ordered benefits shown as a grid (rather than a connected FlowSteps line).

```jsx
<NumberedCard order="01" title="Kickoff" description="Alta técnica y credenciales." />
```
