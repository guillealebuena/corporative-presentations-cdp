One node fanning out into several — decision points, product tiers, or a single integration feeding multiple channels.

```jsx
<FlowBranch root="Carrito CDP" branches={[{label:'Web'},{label:'App'},{label:'Call center'}]} />
```
