Stacked horizontal bands — top layer highlighted in violet, others outlined — for a lightweight architecture/stack diagram.

```jsx
<FlowLayers layers={[{label:'Carrito CDP',detail:'Checkout + pagos'},{label:'API de inventario',detail:'Tu backend actual'},{label:'Operador Fono Bus'}]} />
```
