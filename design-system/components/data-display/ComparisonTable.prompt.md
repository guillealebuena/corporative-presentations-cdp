The comparison table for cost/before-after proposals (layout 05) — situación actual (warm/amber) vs. propuesta CDP (turquoise), alternating rows, ink total row.

```jsx
<ComparisonTable
  rows={[{ label: 'Costo de emisión', current: '$1.200', proposed: '$0' }]}
  total={{ label: 'Ahorro anual estimado', current: '—', proposed: '$4.8M' }}
/>
```

Rule: any slide with more than three data pairs must use this table, never bullets.
