Overlapping rings for comparing overlapping SETS (not a step-by-step process — use Timeline for a sequential process like onboarding).

```jsx
<FlowRings steps={[{icon:'magnifying-glass',label:'Buscar',active:true},{icon:'ticket',label:'Reservar'},{icon:'check-circle',label:'Viajar'}]} />
```
