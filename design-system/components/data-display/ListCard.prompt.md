Ranked row list where each row fades slightly relative to the one above (hierarchy by opacity, not size).

```jsx
<ListCard title="Top rutas" rows={[{label:'BsAs → Mar del Plata',value:'32%'},{label:'BsAs → Rosario',value:'21%'}]} />
```
