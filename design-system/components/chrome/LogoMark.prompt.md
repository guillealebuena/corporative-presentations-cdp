Renders the correct real logo asset for the background it sits on — never recolor or reconstruct the mark.

```jsx
<LogoMark variant="wordmark-blanco" height={26} />
```

Rule: on the brand violet background always use the `-blanco` variant; never violet-on-violet. Minimum size: wordmark 200px wide, isotipo 48px. Keep one logo-height of clear space on all sides.
