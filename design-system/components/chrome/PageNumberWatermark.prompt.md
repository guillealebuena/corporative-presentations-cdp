Huge faint number as a background watermark on data/impact slides — decorative only, doesn't replace SlideFooter's real page number.

```jsx
<div style={{position:'relative'}}><PageNumberWatermark number="06" />...content...</div>
```
