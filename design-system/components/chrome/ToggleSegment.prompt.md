Decorative segmented control — shows which cut of the data a slide is illustrating (not a real interactive control in static decks).

```jsx
<ToggleSegment options={['Mensual', 'Anual']} active={1} />
```
