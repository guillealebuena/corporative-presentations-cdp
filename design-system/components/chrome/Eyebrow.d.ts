/** Small uppercase label with a 24×3px color bar to its left (12px gap) — never a leading em-dash. Always Inter SemiBold. */
export interface EyebrowProps {
  children: React.ReactNode;
  color?: string;
}
export function Eyebrow(props: EyebrowProps): JSX.Element;
